% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{cf_screen}
\alias{cf_screen}
\title{Batch screen compounds from a data frame or CSV file}
\usage{
cf_screen(data, sources = c("pubchem"), admet = FALSE, druglikeness = FALSE, concurrency = 5L)
}
\arguments{
\item{data}{A data frame with at least one of \code{smiles} or \code{name}
columns, or a character string giving the path to a CSV file.}

\item{sources}{Character vector. Database sources to query. Defaults to
\code{c("pubchem")}.}

\item{admet}{Logical. Whether to compute ADMET predictions for each
compound. Defaults to \code{FALSE}.}

\item{druglikeness}{Logical. Whether to compute drug-likeness filters.
Defaults to \code{FALSE}.}

\item{concurrency}{Integer. Maximum concurrent requests. Defaults to 5.}
}
\value{
A tibble with one row per input compound containing the input
identifiers plus retrieved database fields and optionally predicted
ADMET properties.
}
\description{
Searches for multiple compounds in parallel across chemical databases.
Accepts either a data frame with a \code{smiles} or \code{name} column,
or a path to a CSV file with those columns.
}
\examples{
\dontrun{
  # Screen a data frame of SMILES
  df <- data.frame(smiles = c("CCO", "CCC", "CC(=O)O"))
  results <- cf_screen(df)

  # Screen from a CSV file
  results <- cf_screen("compounds.csv", sources = c("pubchem", "chembl"))
}
}
