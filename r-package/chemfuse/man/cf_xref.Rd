% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xref.R
\name{cf_xref}
\alias{cf_xref}
\title{Cross-database identifier mapping}
\usage{
cf_xref(cid = NULL, chembl_id = NULL, smiles = NULL, inchikey = NULL)
}
\arguments{
\item{cid}{Integer or character. PubChem CID. Optional.}

\item{chembl_id}{Character string. ChEMBL identifier (e.g.,
\code{"CHEMBL25"}). Optional.}

\item{smiles}{Character string. SMILES string. Optional.}

\item{inchikey}{Character string. Standard InChIKey (27 chars). Optional.}
}
\value{
A tibble with one row per database mapping. Columns include
\code{database} (database name) and \code{identifier} (mapped ID).
Common databases include \code{pubchem}, \code{chembl},
\code{chebi}, \code{drugbank}, \code{kegg}, \code{hmdb}.
}
\description{
Maps a compound identifier (PubChem CID, ChEMBL ID, SMILES, or InChIKey)
to identifiers in other databases using the UniChem service.

At least one of \code{cid}, \code{chembl_id}, \code{smiles}, or
\code{inchikey} must be provided.
}
\examples{
\dontrun{
  # Map by PubChem CID (aspirin = 2244)
  xref <- cf_xref(cid = 2244)

  # Map by ChEMBL ID
  xref <- cf_xref(chembl_id = "CHEMBL25")

  # Map by SMILES
  xref <- cf_xref(smiles = "CC(=O)Oc1ccccc1C(=O)O")
}
}
